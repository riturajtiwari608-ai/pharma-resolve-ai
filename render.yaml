services:
  - type: web
    name: pharma-resolve-ai
    env: python
    rootDir: backend
    buildCommand: |
      python -m pip install --upgrade pip
      python -m pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    plan: free
